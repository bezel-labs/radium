{
  "name": "radium",
  "version": "0.0.1",
  "private": false,
  "scripts": {
    "build": "npm run tokens && turbo build && rm -rf dist && cp -r apps/storybook/dist dist",
    "dev": "npm run tokens && turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "storybook": "npm run tokens && npm run dev --workspace=@radium/storybook",
    "build:storybook": "npm run tokens && npm run build --workspace=@radium/storybook",
    "tokens": "style-accelerator build",
    "codesandbox:template": "csb build . --ports 5004 --vm-tier Micro --name Radium"
  },
  "devDependencies": {
    "@codesandbox/sdk": "^2.4.2",
    "@keypuncherlabs/style-accelerator": "1.0.1",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "turbo": "^2.8.17",
    "typescript": "5.9.3"
  },
  "packageManager": "npm@10.9.4",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
